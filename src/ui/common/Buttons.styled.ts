import styled from "styled-components";

const Button = styled.span`
    display: block;
    padding: 2rem 0;
    font-size: 2rem;
    line-height: 1.2rem;
    cursor: pointer;
    width: 100%;
`;

export const SpinButtonStyled = styled(Button)`
    background: #F300E8;
`;

export const SwitchChainButtonStyled = styled(Button)`
    background: #00E361;
    color: #000000;
`;
